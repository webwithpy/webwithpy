[tool.poetry]
name = "webwithpy"
version = "0.1.1.1"
description = ""
authors = ["Sven Keimpema"]
readme = "README.md"
packages = [{include = "webwithpy/**/*.*", format = "sdist"}]

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"